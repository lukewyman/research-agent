[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "research-cache"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["redis>=5.0"]

[tool.hatch.build.targets.wheel]
packages = ["research_cache"]
