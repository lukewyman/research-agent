[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "newsrag-cache"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["redis>=5.0"]

[tool.hatch.build.targets.wheel]
packages = ["newsrag_cache"]
